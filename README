Perl Dependencies:
------------------

Install the following Perl modules (e.g. via cpan):
  JSON::Parser
  Data::Dumper (recommended)

Usage:
------

Fetch JSON from WWW:
  curl -k https://map.freifunk-troisdorf.de/nodes.json | ./nodelist

Read local copy of JSON:
  cat nodes.json | ./nodelist

