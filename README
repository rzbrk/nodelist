Perl Dependencies:
------------------

Install the following Pewrl modules (e.g. via cpan):
  JSON::Parser
  Data::Dumper (recommended)

Usage:
------

Fetch JSON from WWW:
  curl -k https://freifunk-troisdorf.de/nodes.json | ./nodelist

Read local copy of JSON:
  cat nodes.json | ./nodelist

